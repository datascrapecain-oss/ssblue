# Front matter & Part/Module intros: cleanup notes

This closes out the scope I flagged earlier (Part-level and Module-level intro pages, which sit outside the numbered sub-pages the original purge request covered).

## What's in this batch

- The landing page (front cover: "What is SourceSheets", real results, explore links)
- Part 1 intro + Module 1 intro
- 1.2 Your Success Path and 1.4 What Tools You'll Use: these two were gaps in the earlier `style-purge-part1.zip` delivery, which only covered 1.1 and 1.3. Combined with that earlier delivery, all of Module 1 (1.1–1.4) is now purged.
- Part 2 intro (Module 2, 3, 4 intros themselves were already done in the Part 2 batch)
- Part 3 intro

## What changed

Same pass as everywhere else: em dashes gone, emoji-bullet headers stripped to plain headings, rule-of-three trimmed, hedging cut. 1.4 also got the SellerAmp/BuyBotPro → ASIN Analyser swap and now describes the analyser as built into SourceSheets rather than a separate tool to install, matching how Module 5 already describes it.

## Still open

- **Module 5's own intro** (the "Your Essential Tools Setup" header that used to sit above the old SellerAmp/BuyBotPro walkthrough) wasn't included in the original `module-5-rework` delivery, that one started straight at 5.1. Since the old intro is entirely about choosing between SellerAmp and BuyBotPro, it needs a full rewrite rather than a purge. Flagging it as a small gap to fill, want me to draft it next time I'm in that area?
- **Module 6, 7, 8 intros**: Module 6's intro was already done (in the original `module-6-rework` delivery). Module 7 doesn't appear to have a separate intro page in the source, 7.2 (already handled in `cross-reference-fixes`) is effectively where the module starts. Module 8's intro was a single line in the batch I just sent, since the source only had a bare title with no body under it, same empty-stub pattern as the old Part 5 pages.
- Part 4 doesn't have its own intro page in the source either, Module 6 starts directly.

At this point, front matter through Part 4 (everything except the old, retired Module 5 intro) has had a style pass. What's left overall: the Module 5 intro rewrite noted above, then the final consistency pass across the whole book once you've had a chance to review what's been delivered so far.
